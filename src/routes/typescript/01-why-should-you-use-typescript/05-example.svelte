<script>
    // JSDoc was something like bridge between Typescript. This is only example.

    const pokemon = [];

    /**
     *
     * @param {string} name Pokemon Name
     */
    function addPokemon(name) {
        pokemon.push(name);
    }
</script>

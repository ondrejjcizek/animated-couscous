<script lang="ts">
    function logPokemon(pokemonList: string[]): void {
        const pokemon = pokemonList.forEach((pokemon) => {
            return pokemon;
        });
    }

    function logPokemonRefactor(pokemonList: string[]): void {
        for (const pokemon of pokemonList) {
            return pokemon;
        }
    }
</script>
